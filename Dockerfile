# Dockerfile - placeholder
# Se completará en la Etapa 7 (Despliegue).
# En la Etapa 1 solo se versiona el archivo vacío para reservar el espacio.

FROM python:3.10-slim

WORKDIR /app

# Las dependencias del proyecto, la copia del código fuente y el
# comando de arranque se definirán en la Etapa 7.