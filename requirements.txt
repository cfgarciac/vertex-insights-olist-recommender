# Dependencias de producción del Proyecto Final
# PINS EXACTOS (D-39): el artefacto producto_promesa_riesgo.joblib se serializó
# con estas versiones (Python 3.11.9); un unpickle con versiones distintas
# puede fallar o degradar silenciosamente. Si se actualiza una versión de
# modelado, hay que re-serializar el artefacto y re-validar métricas.

# === Análisis y manipulación de datos ===
pandas==2.3.3
numpy==2.4.6
pyarrow==24.0.0      # lookups parquet de serving (artifacts/serving/)

# === Modelado (críticos para el unpickle del artefacto) ===
scikit-learn==1.8.0
scipy==1.17.1
xgboost==3.2.0

# === Serialización ===
joblib==1.5.3

# === Serving: API REST (HU-13, D-39) ===
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4

# === Serving: dashboard (HU-15) ===
streamlit==1.57.0
plotly==6.7.0

# === Gráficas de reportes (src/models/evaluate*.py) ===
matplotlib==3.10.9

# === Utilidades de reportes y pruebas ===
reportlab==5.0.0     # scripts/generar_pdf_etapa3.py
pytest==9.1.1        # suite de tests (tests/)
httpx==0.28.1        # TestClient de FastAPI (tests/test_api.py)
